# AI 视频智能重绘 - 依赖列表

# 核心
opencv-python>=4.8.0
numpy>=1.24,<2.0
Pillow>=10.0.0

# PyTorch (CUDA 12.x) - 单独安装，不要在此处 pip install
# pip install torch>=2.3.0 torchvision>=0.18.0 --index-url https://download.pytorch.org/whl/cu121

# Stable Diffusion img2img + ControlNet
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.30.0
controlnet-aux>=0.0.7
xformers>=0.0.26              # 显存优化 (可选)

# Gradio 界面
gradio>=4.20.0

# 工具
tqdm>=4.66
